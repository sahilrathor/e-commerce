{
  "name": "e-commerce",
  "author": "Sahil Rathor",
  "scripts": {
    "client": "yarn --cwd FE dev",
    "server": "yarn --cwd BE dev",
    "build": "yarn --cwd BE install && yarn --cwd FE install && yarn --cwd FE build",
    "dev": "concurrently \"yarn client\" \"yarn server\"",
    "start": "yarn --cwd BE start"
  },
  "version": "1.0.0",
  "main": "index.js",
  "license": "MIT",
  "dependencies": {
    "concurrently": "^9.0.1"
  }
}
